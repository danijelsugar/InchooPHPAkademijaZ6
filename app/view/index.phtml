<form method="post" action="<?php echo App::config('url') ?>Index/newPost" enctype="multipart/form-data">

    <div class="form-group">
        <label for="content">What's on your mind?</label>
        <input id="content" name="content" class="form-control">
    </div>
    <div class="form-group">
        <input type="file" name="image">
    </div>

    <button type="submit" class="btn btn-primary">Save</button>

</form>


<?php foreach ($posts as $post): ?>
    <div class="row">
        <div class="col-4">
            <p>
                <a href="<?php echo App::config('url') ?>Index/view/<?= $post->getId() ?> ">
                    Post: <?= htmlspecialchars($post->getContent()) ?>
                </a>
            </p>
        </div>
        <div class="col-4">
            <p>Time: <?= date('d.m.Y H:i',strtotime($post->getPostTime())); ?></p>
        </div>
        <div class="col-4">
            <p><?= Comment::countComments($post->getId()); ?> comments</p>
        </div>
    </div>
<?php endforeach; ?>
